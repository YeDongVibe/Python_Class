def add(a, b):  # 내부 함수
    return a + b
def subtract(a, b):  # 내부 함수
    return a - b
def multiply(a, b):  # 내부 함수
    return a * b
def divide(a, b):  # 내부 함수
    if (b == 0):
        print("나눌수 없습니다.")
    else:
        return a / b
